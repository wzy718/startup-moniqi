> 处理结果（已按本文建议修正）
> - 已在 `docs/数值/数值类型.md` 统一贷款/债务口径：以 `loans[]` 为唯一真实负债来源，`debt` 仅作派生缓存（剩余本金之和），移除 `debt_interest_rate_annual/debt_weekly_payment` 旧路线。
> - 已在代码侧同步口径：`SaveManager.syncDebtFromLoans()` 在读档、回合开始与贷款周结算后统一回填 `player.debt`，避免 `debt` 与 `loans[]` 双轨不一致。
> - 已在 `docs/数值/数值类型.md` 明确 `delivery_commission` 为派生中间量，并在 13.2 用 `delivery_commission = ...` 的展开写法避免“重复计算”的误读。

一、明确的前后口径冲突（建议优先修）
1️⃣ 贷款系统：debt 与 loans[] 的双轨风险
现状

文档前半：

有 debt

有 debt_weekly_payment

有 debt_interest_rate_annual

后面又推荐使用：

loans[]（等额本息，多笔）

问题

这在设计层面是冲突的，不是“可选”那么简单：

如果 同时存在

debt_weekly_payment

loans[].weekly_payment

那么以下逻辑会混乱：

玩家每周到底被扣几次钱？

压力/事件里引用的 debt 是哪一种？

成就 / 排行里的负债口径不一致

建议（强烈）

二选一，且在文档里写死

推荐你 彻底删除 debt_weekly_payment 这条路线

统一为：

- loans[]：唯一真实负债来源（存档）
- debt：派生值 = Σ loans[].remaining_principal（可存可不存）
- 每周扣款 = Σ loans[].weekly_payment


并在 13.2 成本计算 里明确：

❌ 不再支持“单一 debt + 每周固定还款额”的旧口径

2️⃣ delivery_commission 被计算了两次（文档层面）
位置冲突

3.4.2：

外卖平台抽成：commission = delivery_revenue × commission_rate

7.2：

delivery_cost = delivery_commission + weekly_promo_budget + ...


13.2：

shop_weekly_delivery_cost =
  delivery_revenue × commission_rate
  + weekly_promo_budget
  + platform_fixed_monthly_fee / 4

问题

在阅读层面容易被理解成：

delivery_commission 是一个“已存在字段”，
然后又在 13.2 里重新算了一次

建议（轻度修正）

在文档里统一一句话即可解决：

delivery_commission 是派生中间量，不是存档字段，也不单独参与汇总
